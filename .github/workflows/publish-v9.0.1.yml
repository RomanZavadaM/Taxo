name: Build and publish Taxo 9.0.1

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'VERSION.txt'
      - 'taxo_app.py'
      - 'hotfix_901.py'
      - 'installer/Taxo.iss'
      - 'docs/releases/RELEASE_NOTES_v9_0_1.md'
      - '.github/workflows/publish-v9.0.1.yml'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    env:
      PYTHONUTF8: '1'
    steps:
      - name: Отримати код Taxo 9.0.1
        uses: actions/checkout@v4

      - name: Налаштувати Python 3.13 x64
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          architecture: 'x64'

      - name: Встановити залежності
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller==6.22.2

      - name: Перевірити код і тести
        shell: pwsh
        run: |
          python -m py_compile main.py taxo_app.py work_analysis_ext.py activity_register_60.py v9_release.py hotfix_901.py workspace.py waybill.py tachograph.py attestation_render.py
          python -m unittest discover -s tests -v

      - name: Зібрати Windows Portable
        shell: pwsh
        run: |
          python -m PyInstaller --noconfirm --clean Taxo.spec
          Move-Item 'dist/Taxo' 'dist/Taxo_v9_0_1_Windows_x64'

      - name: Перевірити Portable
        shell: pwsh
        run: |
          $bundle = 'dist/Taxo_v9_0_1_Windows_x64'
          if (!(Test-Path "$bundle/Taxo.exe")) { throw 'Taxo.exe not found' }
          if (!(Test-Path "$bundle/_internal/Бланк підтвердження.docx")) { throw 'DOCX template not found' }
          if (!(Test-Path "$bundle/_internal/attestation_visual_template.pdf")) { throw 'PDF template not found' }
          $db = Get-ChildItem $bundle -Recurse -File -Include *.db,*.sqlite,*.sqlite3
          if ($db) { throw "Database unexpectedly bundled: $($db.FullName -join ', ')" }

      - name: Створити Windows Portable ZIP
        shell: pwsh
        run: Compress-Archive -Path 'dist/Taxo_v9_0_1_Windows_x64' -DestinationPath 'Taxo_v9_0_1_Windows_x64_Portable.zip' -Force

      - name: Зібрати Windows Setup
        shell: pwsh
        run: |
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (!(Test-Path $iscc)) {
            choco install innosetup --no-progress -y
            $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          }
          if (!(Test-Path $iscc)) { throw 'Inno Setup compiler not found' }
          & $iscc 'installer/Taxo.iss'
          if (!(Test-Path 'release_out/Taxo_v9_0_1_Setup_Windows_x64.exe')) { throw 'Installer not found' }

      - name: Завантажити Windows пакети
        uses: actions/upload-artifact@v4
        with:
          name: Taxo-v9.0.1-Windows-x64
          path: |
            Taxo_v9_0_1_Windows_x64_Portable.zip
            release_out/Taxo_v9_0_1_Setup_Windows_x64.exe
          if-no-files-found: error
          retention-days: 30

  build-macos:
    name: Build macOS ${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            arch: arm64
          - runner: macos-15-intel
            arch: x86_64
    env:
      PYTHONUTF8: '1'
    steps:
      - name: Отримати код Taxo 9.0.1
        uses: actions/checkout@v4

      - name: Налаштувати Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Перевірити архітектуру runner
        shell: bash
        run: test "$(uname -m)" = "${{ matrix.arch }}"

      - name: Встановити залежності
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install -r requirements-build-macos.txt

      - name: Перевірити код і тести
        shell: bash
        run: |
          python -m py_compile main.py taxo_app.py work_analysis_ext.py activity_register_60.py v9_release.py hotfix_901.py workspace.py waybill.py tachograph.py attestation_render.py
          python -m unittest discover -s tests -v

      - name: Зібрати і перевірити Taxo.app
        shell: bash
        run: |
          python -m PyInstaller --noconfirm --clean Taxo_macos.spec
          test -x 'dist/Taxo.app/Contents/MacOS/Taxo'
          find 'dist/Taxo.app' -name 'Бланк підтвердження.docx' -print -quit | grep -q .
          find 'dist/Taxo.app' -name 'attestation_visual_template.pdf' -print -quit | grep -q .
          if find 'dist/Taxo.app' -type f \( -name '*.db' -o -name '*.sqlite' -o -name '*.sqlite3' \) -print -quit | grep -q .; then
            echo 'Database unexpectedly bundled in Taxo.app' >&2
            exit 1
          fi
          file 'dist/Taxo.app/Contents/MacOS/Taxo' | grep -q "${{ matrix.arch }}"
          codesign --verify --deep --strict 'dist/Taxo.app'

      - name: Створити macOS Portable ZIP
        shell: bash
        run: |
          bundle_dir="Taxo_v9_0_1_macOS_${{ matrix.arch }}"
          mkdir "${bundle_dir}"
          mv 'dist/Taxo.app' "${bundle_dir}/Taxo.app"
          /usr/bin/ditto -c -k --sequesterRsrc --keepParent "${bundle_dir}" "${bundle_dir}_Portable.zip"

      - name: Завантажити macOS пакет
        uses: actions/upload-artifact@v4
        with:
          name: Taxo-v9.0.1-macOS-${{ matrix.arch }}
          path: Taxo_v9_0_1_macOS_${{ matrix.arch }}_Portable.zip
          if-no-files-found: error
          retention-days: 30

  publish-release:
    needs:
      - build-windows
      - build-macos
    runs-on: ubuntu-latest
    steps:
      - name: Отримати точний код 9.0.1
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Завантажити перевірені пакети
        uses: actions/download-artifact@v4
        with:
          pattern: Taxo-v9.0.1-*
          path: release-assets
          merge-multiple: true

      - name: Підготувати START-пакет і контрольні суми
        shell: bash
        run: |
          mkdir release-upload
          find release-assets -type f -name 'Taxo_v9_0_1_Setup_Windows_x64.exe' -exec cp '{}' release-upload/ \;
          find release-assets -type f -name 'Taxo_v9_0_1_Windows_x64_Portable.zip' -exec cp '{}' release-upload/ \;
          find release-assets -type f -name 'Taxo_v9_0_1_macOS_arm64_Portable.zip' -exec cp '{}' release-upload/ \;
          find release-assets -type f -name 'Taxo_v9_0_1_macOS_x86_64_Portable.zip' -exec cp '{}' release-upload/ \;
          git archive --format=zip --prefix='Taxo_v9_0_1_START/' --output='release-upload/Taxo_v9_0_1_START.zip' HEAD
          test -f 'release-upload/Taxo_v9_0_1_Setup_Windows_x64.exe'
          test -f 'release-upload/Taxo_v9_0_1_Windows_x64_Portable.zip'
          test -f 'release-upload/Taxo_v9_0_1_macOS_arm64_Portable.zip'
          test -f 'release-upload/Taxo_v9_0_1_macOS_x86_64_Portable.zip'
          test -f 'release-upload/Taxo_v9_0_1_START.zip'
          if unzip -l 'release-upload/Taxo_v9_0_1_START.zip' | grep -Eiq '\.(db|sqlite|sqlite3)([[:space:]]|$)|__pycache__|\.pyc([[:space:]]|$)'; then
            echo 'Database or cache unexpectedly bundled in source package' >&2
            exit 1
          fi
          if unzip -l 'release-upload/Taxo_v9_0_1_START.zip' | grep -Eiq '/(CHECKPOINT_v8|TEST_REPORT_v8|PUBLISH_STATUS_v8|PATCH_v8)'; then
            echo 'Legacy development clutter unexpectedly bundled in START package' >&2
            exit 1
          fi
          cd release-upload
          sha256sum \
            Taxo_v9_0_1_Setup_Windows_x64.exe \
            Taxo_v9_0_1_Windows_x64_Portable.zip \
            Taxo_v9_0_1_macOS_arm64_Portable.zip \
            Taxo_v9_0_1_macOS_x86_64_Portable.zip \
            Taxo_v9_0_1_START.zip \
            > SHA256SUMS_v9_0_1.txt
          sha256sum -c SHA256SUMS_v9_0_1.txt

      - name: Опублікувати стабільний реліз v9.0.1
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if gh release view v9.0.1 >/dev/null 2>&1; then
            gh release edit v9.0.1 \
              --title 'Taxo 9.0.1 — стабільне виправлення' \
              --notes-file docs/releases/RELEASE_NOTES_v9_0_1.md \
              --latest
          else
            gh release create v9.0.1 \
              --target "$GITHUB_SHA" \
              --title 'Taxo 9.0.1 — стабільне виправлення' \
              --notes-file docs/releases/RELEASE_NOTES_v9_0_1.md \
              --latest
          fi
          gh release upload v9.0.1 release-upload/* --clobber

      - name: Зберегти START і manifest як workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Taxo-v9.0.1-release-manifest
          path: |
            release-upload/Taxo_v9_0_1_START.zip
            release-upload/SHA256SUMS_v9_0_1.txt
          if-no-files-found: error
          retention-days: 90
