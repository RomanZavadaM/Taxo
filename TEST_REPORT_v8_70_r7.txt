Taxo v8.70 candidate r7 — test report
Date: 2026-09-15

PASS — Python compile: main.py, waybill.py, tachograph.py.
PASS — 17 unittest cases (r5 + r6 + r7 compatibility suite).
PASS — schema contains optional planned_distance_km on routes and waybills.
PASS — blank route distance accepted; invalid/zero values rejected without affecting older routes.
PASS — odometer-end forecast is calculated but never stored as actual data.
PASS — plan/actual tolerance: warning only beyond max(10 km, 10% of plan).
PASS — waybill INSERT/UPDATE placeholder counts match their argument tuples.
PASS — two-page vector A4 landscape PDF rendered and visually inspected.
PASS — planned 275 km printed separately from actual 280 km; calendar dates remain intact.
PASS — no executable packaging requested or performed; routine candidate uses START.bat.

GitHub CI: pending publication.
