# Taxo — стан проєкту

Контрольна версія: **v8.56**  
Дата перевірки: **11.09.2026**  
Наступна версія для змін: **v8.57**

## Джерело істини

Контрольний локальний/Library-реліз: `Taxo_v8_56.zip`.
SHA-256 ZIP: `c1056a771457c4d499be2b2ff70eef45acdccc2bc9e2047fd7ca9aed2d533881`.

У GitHub гілка `release-v8.56` відновлює той самий код через перевірену базу v8.41 та текстові патчі v8.45 → v8.49 → v8.56. `main.py` виконує SHA-256 контроль відновлених `main.py` і `tachograph.py` перед запуском.

Контрольні SHA-256 кінцевих файлів v8.56:

- `main.py`: `30c5d46974964be86ee3a0d72130f20ef7c63893c7e82e30b8484abda014815f`
- `tachograph.py`: `6018a71b4a283dbbba640e1daa313eff9f26cdde931aa4cba77884a5141d20cc`
- `Бланк підтвердження.docx`: `47aeb545509af8d43102f76de772838b36de1fa85a2d09bae2d2ce7798358ec0`
- `requirements.txt`: `09acc97b0f5b65cab55a2dfb27304107769283154f81209bf5f65b3b30c9f319`
- `START.bat`: `34e20856ca85902b9a4b8beef5ef51c30cd86bdac3c83b9db23340606506cfb9`

## Підтверджені зміни v8.50–v8.56

### v8.50
- Додано режими дня: `tacho`, `no_tacho_8h`, `manual`.
- Додано контроль Бланків підтвердження за 56-денним вікном.

### v8.51–v8.53
- Усі частини одного маршрутного ТАХО-дня утворюють один блок від першого виїзду до останнього повернення.
- Внутрішні паузи між частинами дня не створюють окремих бланків.
- День `Без тахо — стандартні 8 год` між ТАХО-маршрутами враховується в міжмаршрутному проміжку.
- Тахокарта не може змінювати `worklog` або `work_segments`; вона є лише вибірковим контролем.
- Протоколи тахоконтролю зберігаються окремо в `tachograph_test.sqlite3`.

### v8.54
- Автоматичний поділ проміжку за видами діяльності.
- Автокоди: 14 — лікарняний, 15 — відпустка, 16 — відпочинок/вихідний, 18 — `Без тахо — 8 год` / інша робота, 19 — доступний.
- Змішаний проміжок може автоматично ділитися, наприклад, `16 → 18 → 16`.

### v8.55
- Дата Бланка підтвердження завжди дорівнює календарній даті завершення його періоду (`Період по`).
- Поле дати у формі автоматичне/тільки для читання.

### v8.56
- Історичні незакриті проміжки лишаються аварійним контролем.
- Додано поточний період до найближчого наступного запланованого ТАХО-виїзду.
- Статуси: `ПОТОЧНИЙ — ПІДГОТУВАТИ` та `ПОТОЧНИЙ — БЛАНК ГОТОВИЙ`.
- Для двох послідовних календарних ТАХО-днів зайвий бланк не формується.
- Поточний змішаний проміжок зберігає автокоди 14/15/16/18/19.

## Незмінні правила

- Основна БД користувача не входить у ZIP і не повинна потрапляти на GitHub.
- Постійні дані користувача: `%USERPROFILE%\Documents\DriverWorktime\`.
- Дати в UI: `ДД.ММ.РРРР`.
- Друк офісних таблиць: A4; широкі місячні графіки/табелі — A4 landscape.
- У місячному табелі / балансі в денних клітинках показуються **тільки години** (`8:00`, `7:30`).
- За один період Бланка підтвердження обирається рівно одна позиція 14–19; позиція 16 є типовим значенням відпочинку.
- Тахокарти — тільки вибірковий контроль, без автоматичного виправлення основного графіка.

## Контроль v8.56

Перевірено 11.09.2026:

- `main.py` і `tachograph.py` проходять `py_compile`;
- поточний сценарій ТАХО 09.09 до 18:00 → наступний виїзд 12.09 06:00 показує `ПОТОЧНИЙ — ПІДГОТУВАТИ`;
- послідовні календарні ТАХО-дні не створюють поточного бланка;
- змішаний сценарій `Без тахо — 8 год` ділиться на `16 → 18 → 16`;
- підготовлений поточний бланк визначається як `ПОТОЧНИЙ — БЛАНК ГОТОВИЙ`;
- тестовий DOCX формувався з датою документа, що дорівнює даті завершення періоду;
- дві незалежні копії `Taxo_v8_56.zip` у Library мають однаковий розмір і однаковий SHA-256;
- GitHub-патчі v8.56 байт-у-байт відновлюють еталонні `main.py` і `tachograph.py` з ZIP.

## Правило подальшої розробки

1. v8.56 більше не редагувати після завершення публікації.
2. Наступні зміни робити як v8.57.
3. Після змін: compile + smoke tests + новий ZIP з унікальною внутрішньою папкою.
4. Оновлювати `PROJECT_STATE.md` і контрольні SHA-256.
5. Зберігати контрольний ZIP щонайменше у двох незалежних місцях.
