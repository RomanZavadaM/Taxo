# Taxo — стан проєкту

Контрольна опублікована/відновлена версія: **v8.60**  
Ланцюжок відновлених кандидатів: **v8.57 → v8.58 → v8.59 → v8.60**  
Дата складання й повторної перевірки v8.60: **11.09.2026**  
Наступна версія для змін: **v8.61**

## Джерело істини

Робочими файлами програми є:

- `main.py` — основна програма;
- `tachograph.py` — модуль вибіркового контролю аналогових тахокарт;
- `Бланк підтвердження.docx` — шаблон Додатка 3;
- `requirements.txt` — залежності;
- `START.bat` — запуск на Windows.

`START.bat` запускає саме `main.py`.

Файли `main_github.py` та `part1.txt` … `part4.txt` збережені як історичні/службові артефакти попередньої схеми публікації на GitHub. Вони **не є робочою версією v8.57** і не запускаються `START.bat`.

## Підтверджені зміни v8.48–v8.56

### v8.48
- У місячному табелі збільшено назву підприємства.
- У бланку заповнюються нижні поля місця/дати водія, у тому числі англійською.

### v8.49
- Єдине поле місця — місце директора/представника перевізника.
- Нижній блок водія автоматично дублює це місце.

### v8.50
- Додано `accounting_mode` для дня:
  - `tacho` — ТАХО / маршрут / шаблон;
  - `no_tacho_8h` — без тахо, стандартні 8 год;
  - `manual` — ручний облік.
- Додано контроль Бланків підтвердження за 56-денним вікном.

### v8.51
- Усі частини одного маршрутного ТАХО-дня утворюють один блок від першого виїзду до останнього повернення.
- Внутрішні паузи між частинами дня не створюють окремих бланків.
- Один бланк пропонується на весь проміжок між двома послідовними ТАХО-маршрутами.

### v8.52
- Кнопку у контролі перейменовано на `Сформувати Бланк підтвердження`.
- Позиція 16 — лише значення за замовчуванням, а не номер окремого виду бланка.

### v8.53
- День `Без тахо — стандартні 8 год`, якщо він лежить між двома ТАХО-маршрутами, не вирізається з міжмаршрутного проміжку.
- Тахокарта більше не може змінювати `worklog` або `work_segments`.
- У модулі тахографа є `Зберегти протокол контролю` і вікно історії протоколів.
- Протоколи зберігаються лише в окремій `tachograph_test.sqlite3`.

### v8.54
- Виправлено зайві бланки між кількома ТАХО-робочими днями підряд.
- Між послідовними календарними ТАХО-днями нічний/міжзмінний відпочинок не вимагає автоматичного бланка.
- Якщо між ТАХО-днями є інший календарний день, автоматичний контроль закриває проміжок бланком(ами).
- Автокоди: лікарняний 14, відпустка 15, вихідний/відпочинок 16, `Без тахо — 8 год`/інша робота 18, доступний 19.
- Зміна виду діяльності всередині одного міжтахографного проміжку розбиває його на окремі бланки.
- Вибір рядка в контролі автоматично встановлює його запропонований код, але код можна змінити вручну.
- 56 днів лишаються тільки вікном контролю, а не вимогою заповнити бланками весь міжтахографний час.

### v8.55
- Дата Бланка підтвердження **завжди дорівнює календарній даті закінчення періоду**, на який сформовано цей бланк.
- Це правило примусово застосовується в єдиній функції створення документа, тому однаково працює для ручного створення і для 56-денного контролю.
- Поле дати у формі стало автоматичним/тільки для читання; воно синхронізується з полем `Період по`.
- День контролю більше не може випадково потрапити в поле дати сформованого бланка.

### v8.56
- Історичні незакриті проміжки лишаються в контролі як аварійне відновлення.
- Додано окремий **поточний період до наступного виїзду**: програма дивиться вперед у графік, знаходить найближчий запланований ТАХО-виїзд і пропонує бланк ще до виїзду.
- Поточний рядок має статус `ПОТОЧНИЙ — ПІДГОТУВАТИ`; вже підготовлений бланк — `ПОТОЧНИЙ — БЛАНК ГОТОВИЙ`.
- Якщо наступний ТАХО-день іде одразу наступного календарного дня, бланк як і раніше не формується.
- Для поточного змішаного проміжку зберігаються автокоди 14/15/16/18/19 і поділ за зміною виду діяльності.
- Дата поточного бланка лишається датою завершення його періоду, навіть якщо документ друкується заздалегідь перед виїздом.

### v8.57 — кандидат
- Додано редагування вже сформованого Бланка: період, позиція 14–19 та місце.
- Редагування створює нову ревізію та новий DOCX; попередній DOCX переноситься у контрольний архів `AttestationArchive/Replaced`.
- Додано контрольоване вилучення: запис отримує статус `deleted`, перестає враховуватися у 56-денному/поточному контролі, але фізично не стирається.
- DOCX вилученого бланка переноситься у `AttestationArchive/Deleted`.
- Додано відновлення вилученого Бланка.
- Додано таблицю `attestation_audit` та номер ревізії; журнал фіксує BASELINE/CREATE/EDIT/DELETE/RESTORE.
- Вилучені записи приховані за замовчуванням, але доступні через прапорець `Показувати вилучені`.
- Перед EDIT/DELETE/RESTORE автоматично створюється резервна копія БД.
- Виправлено очищення Бланків за правилом 48 місяців: `period_to` розбирається як дата, а не порівнюється текстово.

### v8.58 — кандидат
- На сторінці `Підприємство` додано окремий блок реквізитів для англійської сторони Бланка.
- Нові поля: `name_en`, `address_en`, `signer_name_en`, `signer_position_en`, `place_en`.
- Для кожного поля діє fallback: порожнє англійське поле → відповідне українське значення.
- Телефон, факс і E-mail використовуються однаково на обох сторонах і не дублюються в UI.
- Англійська сторона Бланка бере англійські значення для назви підприємства, адреси, ПІБ представника, посади та місця; українська сторона не змінена.
- Міграція старої БД автоматично додає нові поля без зміни вже введених реквізитів.

## Інші важливі правила, які не можна втрачати

- Основна БД користувача не входить у ZIP і не повинна потрапляти на GitHub.
- Постійні дані: `%USERPROFILE%\Documents\DriverWorktime\`.
- Дати в UI: `ДД.ММ.РРРР`.
- Друк офісних таблиць: **A4**; широкі місячні графіки/табелі — **A4 landscape**.
- У місячному табелі / балансі в денних клітинках показуються **тільки години** (`8:00`, `7:30`), а не час початку/кінця зміни.
- Бланк підтвердження: за замовчуванням позиція 16; за один період обирається рівно одна позиція 14–19.
- Дата Бланка підтвердження завжди = дата завершення його періоду (`Період по`).
- Тахокарти — тільки вибірковий контроль, без автоматичного виправлення основного графіка.
- Місячний графік Taxo не слід називати повною офіційною формою Додатка 1 на 56 днів + день контролю.

## Перевірка відновленої v8.53

Виконано 11.09.2026 на ізольованій тестовій БД:

- `main.py`, `tachograph.py`, `main_github.py` — `py_compile` успішний;
- схема основної БД створюється, поле `accounting_mode` присутнє;
- сценарій: ТАХО 09.09 06:00–14:00, `Без тахо — 8 год` 10.09, ТАХО 11.09 з 06:00;
- контроль правильно визначив **один проміжок 40:00**;
- після внесення одного бланка на 09.09 14:00 → 11.09 06:00 проміжок визначився як повністю закритий;
- створення DOCX Бланка підтвердження успішне;
- PDF та Excel місячного табеля формуються;
- у тахографічній БД створюються таблиці `discs`, `intervals`, `control_protocols`;
- у релізному ZIP немає `.sqlite`, `.sqlite3`, `.db` і `__pycache__`.

## Перевірка v8.54

Виконано 11.09.2026 на ізольованій тестовій БД:

- `main.py` і `tachograph.py` проходять `py_compile`;
- три ТАХО-дні підряд: **0 бланків** між ними;
- ТАХО → два вихідні → ТАХО: **один бланк позиції 16** від кінця попереднього маршруту до початку наступного;
- ТАХО → `Без тахо — 8 год` → ТАХО: автоматично три частини **16 → 18 → 16**;
- відпустка/лікарняний/доступний автоматично дають **15/14/19**;
- існуючий бланк із вручну зміненим кодом все одно вважається свідомим покриттям часу;
- тестовий DOCX Бланка підтвердження успішно формується.

## Перевірка v8.55

Виконано 11.09.2026 на ізольованій тестовій БД:

- `main.py` і `tachograph.py` проходять `py_compile`;
- при періоді `18:00 01.09.2026 → 06:00 04.09.2026` навмисно передана неправильна дата `01.01.2000` була проігнорована, а в БД і DOCX записано **04.09.2026**;
- для періоду через північ `23:00 04.09.2026 → 02:00 05.09.2026` дата бланка = **05.09.2026**;
- в українській та англійській частинах тестового DOCX дата однакова і відповідає кінцю періоду;
- регресійна перевірка v8.54: три ТАХО-дні підряд як і раніше не створюють зайвих бланків;
- тестовий DOCX відрендерено у 2 сторінки та візуально перевірено.

## Перевірка v8.56

Виконано 11.09.2026 на ізольованій тестовій БД:

- історичний сценарій ТАХО → два вихідні → ТАХО як і раніше дає один аварійний бланк позиції 16;
- поточний сценарій: ТАХО 09.09 до 18:00, наступний виїзд за графіком 12.09 о 06:00 — контроль показує `ПОТОЧНИЙ — ПІДГОТУВАТИ` на 09.09 18:00 → 12.09 06:00;
- два послідовні календарні ТАХО-дні не створюють поточного бланка;
- поточний змішаний сценарій з `Без тахо — 8 год` правильно ділиться на 16 → 18 → 16;
- вже підготовлений поточний бланк визначається як `ПОТОЧНИЙ — БЛАНК ГОТОВИЙ` і не попереджається вдруге;
- тестовий DOCX поточного відпочинку створено на майбутній момент виїзду, дата документа = дата завершення періоду, обидві сторони відрендерено та візуально перевірено;
- `main.py` і `tachograph.py` проходять `py_compile`.

## Перевірка v8.57

Виконано 11.09.2026 на ізольованих тимчасових БД:

- `main.py` і `tachograph.py` проходять `py_compile`;
- міграція БД v8.56 додає `status`, `revision`, `updated_at`, `deleted_at`, `delete_reason` без втрати старих Бланків;
- створюється таблиця `attestation_audit`;
- активний Бланк повністю закриває тестовий поточний проміжок; після статусу `deleted` той самий проміжок знову має `ПОТОЧНИЙ — ПІДГОТУВАТИ`;
- редагування створює ревізію 2, новий DOCX та переносить старий DOCX у `AttestationArchive/Replaced`;
- вилучення створює ревізію 2 і запис DELETE, відновлення — ревізію 3 і RESTORE; файл після обох операцій зберігається;
- 48-місячне очищення правильно вилучає Бланк вересня 2022 та залишає Бланк жовтня 2022 при контрольній даті вересня 2026;
- тестові операції виконувалися тільки на тимчасових SQLite-БД, база користувача не використовувалась.

## Перевірка v8.58

Виконано 11.09.2026 на ізольованій тестовій БД та тестових DOCX:

- `main.py` і `tachograph.py` проходять `py_compile`;
- міграція старої схеми `company` автоматично додає `name_en`, `address_en`, `signer_name_en`, `signer_position_en`, `place_en`;
- якщо англійські поля порожні, англійська сторона DOCX правильно використовує українські значення;
- якщо англійські поля заповнені, DOCX бере саме їх;
- телефон, факс та E-mail на англійській стороні копіюються без змін;
- окреме англійське `Place` використовується і в п.20, і в нижньому блоці водія;
- GUI-поля англійських реквізитів зберігаються та повторно завантажуються з БД;
- тестовий DOCX відрендерено у 2 сторінки та візуально перевірено: переповнення, обрізання і зсувів немає;
- тестові операції виконувалися тільки на тимчасовій SQLite-БД.

## Перевірка v8.59

Виконано 11.09.2026 після повідомлення користувача, що введена англійська назва не потрапила у Бланк:

- встановлена причина: у v8.58 поля були на вкладці «Підприємство», але видима кнопка збереження на самій вкладці була відсутня; збереження було тільки в меню «Файл»;
- додано видиму кнопку `Зберегти реквізити підприємства`;
- перед створенням нового Бланка реквізити з GUI автоматично зберігаються без додаткового діалогу;
- перед редагуванням/перегенерацією існуючого Бланка реквізити також автоматично зберігаються;
- порожнє англійське поле як і раніше використовує українське значення; заповнене — англійське;
- `main.py` і `tachograph.py` проходять `py_compile`;
- GUI-сценарій «ввести `name_en` → НЕ натискати Save → одразу створити Бланк» перевірено окремо: англійська назва з'являється в DOCX і зберігається в SQLite.

## Перевірка v8.60

Виконано 11.09.2026:

- `main.py` і `tachograph.py` проходять `py_compile`;
- стара БД автоматично отримує `last_name_en`, `first_name_en`, `middle_name_en`;
- заповнені англійські ПІБ використовуються на англійській стороні Бланка;
- перевірено поелементний fallback англійських ПІБ на українські;
- англійські реквізити підприємства з v8.59 залишилися робочими;
- база користувача у ZIP відсутня.

## Правило подальшої розробки

1. **v8.60 є поточною контрольною точкою. Її не редагувати.**
2. Будь-які наступні зміни починати тільки з копії v8.60 як `Taxo_v8_61`.
3. Перед початком роботи завжди прочитати `RECOVERY_INDEX.md`, `AI_HANDOFF.md` і цей `PROJECT_STATE.md`; номер версії не визначати з пам’яті чату.
4. Після змін — `py_compile` + smoke tests + перевірка DOCX/PDF/JPG, якщо змінювався Бланк.
5. Кожна версія отримує нову внутрішню папку, `VERSION.txt`, `SHA256SUMS.txt`, тест-звіт і контрольний ZIP.
6. База користувача не входить у реліз і не потрапляє на GitHub.
7. Контрольний ZIP зберігати щонайменше у двох незалежних місцях: Library + локально/GitHub.
8. На GitHub для великих ZIP дозволено зберігати відновлювану Base64-копію частинами з SHA-256.
9. Перед переходом до нової функції переконатися, що контрольна версія присутня в Library і на GitHub та її SHA-256 зафіксований.

## v8.60 — англійські дані водія

- У картці водія додано окремі поля: прізвище, ім'я та по батькові англійською.
- На англійській стороні Бланка кожне поле має власний fallback: якщо англійське значення порожнє, використовується відповідне українське.
- Українська сторона Бланка та інші звіти продовжують використовувати українські ПІБ.
- Стара база автоматично доповнюється полями `last_name_en`, `first_name_en`, `middle_name_en`; міграція даних вручну не потрібна.
