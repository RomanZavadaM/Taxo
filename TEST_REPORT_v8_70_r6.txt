Taxo v8.70 candidate r6 — TEST REPORT
Date: 2026-09-15

PASS — Python syntax compilation for main.py, waybill.py, tachograph.py and attestation_render.py.
PASS — 13 automated unittest cases (8 preserved r5 tests + 5 new r6 tests).
PASS — additive creation of employee_time_entries and vehicle_odometer_readings.
PASS — overnight employee shift split across two calendar days.
PASS — optional odometer parsing, history persistence and warning-only consistency checks.
PASS — two-page vector A4 waybill prints start/end odometer values and calculated distance.
PASS — PDF has two pages, embedded Ukrainian fonts, no raster scan background and real calendar dates.
PASS — routine GitHub checks do not build executables; packaging remains workflow_dispatch-only.

MANUAL CHECK THROUGH START.bat:
  open Працівники → Табель персоналу and edit one daily record;
  use План → факт and then clear the manual row;
  open Графік водіїв → Шляхівки на день → Спідометр / пробіг;
  save empty, start-only and start+end values;
  reprint a waybill and confirm the values on page two;
  enter a smaller value and confirm that it warns without blocking.
